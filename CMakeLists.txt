CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(libtq CXX)
INCLUDE_DIRECTORIES(include src)

ADD_DEFINITIONS(-g)

ADD_LIBRARY(tq
  STATIC
  src/task_queue.cc
  src/task_desc.cc)

TARGET_LINK_LIBRARIES(tq pthread)
